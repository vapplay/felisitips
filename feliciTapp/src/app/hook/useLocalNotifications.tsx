import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const useLocalNotifications = () => {
  return (
    <View>
      <Text>useLocalNotifications</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
